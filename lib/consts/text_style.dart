import 'package:flutter/material.dart';

customTextStyle({double? size, color, weight, family}) {
  return TextStyle(
    fontSize: size,
    color: color,
    fontWeight: weight,
    fontFamily: family,
  );
}
